
@{
    ViewBag.Title = "CargarAntecedente";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script>
    $(document).ready(
        function () {
            $("#msj").hide();
            @*$("#btnGuardar").click(
                function () {
                    $.ajax({
                        url: '@Url.Action("CrearCentro", "User")',
                        type: 'POST',
                        data: $("#formulario").serialize(),
                        success: function (result) {
                            //alert(result);
                            if (result === 'true') {
                                $("#msj").show(2000).delay(2000).fadeOut(2000);
                                setTimeout(function () { $(location).attr('href', '@Url.Action("PanelAdmin", "User")'); }, 4000);
                            }
                            else {
                                alert('problema');
                            }
                        }
                    }
                    );
                }

            );*@
        }
    );
</script>
<body>
    <div id="msj" class="alert alert-success">
        Centro registrado con exito
    </div>
    <center>
        <h2>CargarAntecedente</h2>
        <form id="formulario" action="@Url.Action("SubirAntecedente","User")" method="post" enctype="multipart/form-data">
            <table>
                <tr>
                    <td><input type="file" id="archivo" name="archivo"  value="Subir"/></td>
                    <td></td>
                </tr>

                <tr>
                    <td>Tipo de documento</td>
                    <td><input class="form-control" type="text" id="tipo" name="tipo" /></td>
                </tr>
                <tr>
                    <td></td>
                    <td><input type="submit" class="btn btn-primary" id="btnGuardar" name="btnGuardar" value="Guardar" /></td>
                </tr>
            </table>
        </form>
    </center>
</body>

